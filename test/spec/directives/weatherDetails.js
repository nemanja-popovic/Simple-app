'use strict';

describe('Directive: weatherDetails', function () {

  // load the directive's module
  beforeEach(module('HybrisApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
